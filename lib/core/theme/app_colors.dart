import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand gold — matches #f2c240 on pashtuncollections.in
  static const primary    = Color(0xFFF2C240);
  // Pure black — buttons, headers
  static const secondary  = Color(0xFF000000);
  // Light grey — section backgrounds, cards
  static const accent     = Color(0xFFF5F5F5);
  static const saleRed    = Color(0xFFDD0000);
  static const surface    = Color(0xFFFFFFFF);
  static const background = Color(0xFFFFFFFF);
  static const textPrimary   = Color(0xFF000000);
  static const textSecondary = Color(0xFF6E6E71);
  static const textHint      = Color(0xFFB1B1B1);
  static const divider       = Color(0xFFE5E5E5);
  static const shimmerBase      = Color(0xFFF0F0F0);
  static const shimmerHighlight = Color(0xFFFFFFFF);
}
